# Model directory

Final GGUF weights are deliberately excluded from Git. `download_model.sh` must place the exact public model at `model/methodbridge-local-final.gguf` and verify its SHA-256. No smoke or placeholder model may be described as the final candidate.
