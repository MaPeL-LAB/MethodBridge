# Security policy

Report vulnerabilities privately to the repository owner rather than opening a public issue containing exploit details.

The repository treats model files, download URLs, source datasets, prompts, dependency locks, and evaluation results as supply-chain assets. Verify hashes, avoid credential-bearing URLs, keep inference offline, and never log participant data or secrets. Local retrieval is deferred because it adds indirect prompt-injection and data-governance risk to the judged path.
